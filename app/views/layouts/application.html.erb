<!DOCTYPE html>
<html class="dark">
  <head>
    <title><%= content_for(:title) || "Delta Neutral" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Delta Neutral">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-950 text-gray-100 min-h-screen">
    <% if authenticated? %>
      <nav class="bg-gray-900 border-b border-gray-800">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex items-center justify-between h-16">
            <div class="flex items-center space-x-8">
              <span class="text-xl font-bold text-white tracking-tight">Delta Neutral <span class="text-xs font-normal text-gray-500">v<%= DeltaNeutral::VERSION %></span></span>
              <%= link_to "Dashboard", root_path, class: "text-gray-300 hover:text-white transition text-sm font-medium" %>
              <%= link_to "Positions", positions_path, class: "text-gray-300 hover:text-white transition text-sm font-medium" %>
              <%= link_to "Mellow Autopilot", mellow_autopilot_probe_path, class: "text-gray-300 hover:text-white transition text-sm font-medium" %>
              <%= link_to "Wallets", wallets_path, class: "text-gray-300 hover:text-white transition text-sm font-medium" %>
              <%= link_to "Settings", edit_settings_path, class: "text-gray-300 hover:text-white transition text-sm font-medium" %>
            </div>
            <div class="flex items-center space-x-4">
              <span class="text-gray-400 text-sm leading-none"><%= Current.user.first_name %> <%= Current.user.last_name %></span>
              <%= button_to "Logout", session_path, method: :delete, class: "text-gray-400 hover:text-white text-sm font-medium transition leading-none p-0 m-0", form_class: "flex items-center m-0 p-0" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <% if flash[:notice] %>
        <div class="mb-6 rounded-lg bg-green-900/50 border border-green-700 p-4 text-green-300 text-sm">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="mb-6 rounded-lg bg-red-900/50 border border-red-700 p-4 text-red-300 text-sm">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
