services:
  web:
    build: .
    ports:
      - "43080:80"
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      - RAILS_ENV=production
      - SOLID_QUEUE_IN_PUMA=1
    volumes:
      - type: bind
        source: ./storage
        target: /rails/storage
